import 'package:flutter/material.dart';

class QuizInfor extends StatelessWidget {
  const QuizInfor({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      
    );
  }
}